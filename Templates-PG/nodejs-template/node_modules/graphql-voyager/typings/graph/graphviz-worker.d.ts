export declare class VizWorker {
    private _cache;
    private _worker;
    private _listeners;
    constructor();
    renderString(dot: string): Promise<string>;
    generateCacheKey(dot: string): Promise<string | null>;
    _renderString(src: string): Promise<string>;
}
